{
    "name": "portfolio-pro-root",
    "version": "1.0.0",
    "scripts": {
        "build": "cd frontend && npm install && npm run build",
        "postinstall": "cd backend && npm install && cd ../frontend && npm install"
    }
}